* Created The project 
* Renamed the Project name fromt he index.html file
* Installed tailwind Css
* Configuration in vite.config adding tailwindCss in it
* Clear the assets and add dummy data to it for continueing with dummy detials
* Install react router dom, lucide react, react hot toast from the terminal
* Instead of strict mode in main.jsx we imported BrowserRouter and placed it insead of react
----- Now we setup the basic react project ------
* Now we will connect the code review tool with our project for that we are using CodeRabbit==> it finds the issue and suggest improvement This keeps code base clean . There is CLI feature helps to review your code in your OS terminal it supports all operating system.
* Created a new branch and named it as development 
* Setup Toaster in App.jsx and set the routers and there routes nested 
* <Route path="*" element={<Navigate to="/dashboard" replace/>} /> == if we select any other routes other than mentioned routes it will go to the Navigate to dashboard  as the setting which is mentioned is in path "*".
* At the app we changed the size of layout , the layout was visible along all the routes ,so adjusted the size of the layoutpage and maid the layout page a part of the all the other nested routes.
----- Create component for the login page ------
* At app.jsx set up the login routes for admin and employee form there itself add meta data like role,title,subtitle etc.
* And started working in two components LoginLeftside this componet is reused in multiple pages like login landing and login for employee and admin
* We used lucide-react to import the icond such as arrow icon admin icon user icon etc.
---- After setting up the landing login pages started developing the login form and in the login form component . from this component we are using useState for the first time ----
*Link tag to write the path and icon will be written in the shown ui side 
*the values of titile and subtitle passed from the app as props are taken from here and displayed 
*widely used loader icons eye icons to show password button and all 
*----Compleated the Login UI after creation used code rabbit to analyse or test the code through git and corrected all the changes and merged the push----
*Now going to create the Pages after login in every pages Sidebarcomponent is a important one so we are creating that first 
*To create we use useLocation hook to track the path we use useState to set the username.
*We use useEffect hook to show the full userName when loaded . and another useEffect to turn off the sidebar immidatly when the page changes.
-----SideBar----
Userprofile is found by checking wether there is a username exist
Nav items are sotred as a array of objects later thay are mapped in the side bar navigation part teh details such as name route and icon are provided in the object itself its mapped and shown in the side bar and there is a ternary operator which works when the user changes form empoyee to admin.
As a part of seting up the dashboard now the exact logic comes in to picture Implemented loading component.
---Created a emplyeedashboard-----
in the dashboard there is a option to check the role of the user if it is a admin it uses admin component else employee componets now created employee components
in the employee components we are getting thee data as props from the dashboard in the Dashboard side it as state using the useEffect hook with a time delay to fectch that time delay is compromised by a loading animation. 
Here they are setting the dummy employee data to the data fromt he asset and passing it as props to the employee after checking the user is an empleyeee 
In employee dashboard the data is splitted. Here in the employee dashbord we create a array for cards and the cards contains the details of the employee which is passed throught the dummy data.
and the array of object Cards. has icon value title and subtitle destructured  for displaying 
at the end of the componet we set up two link to connect to the other rout for marking the attendance and to apply leave.


